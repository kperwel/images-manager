import React from "react";
import styled from "styled-components";

const TopBarStyled = styled.div`
  background: #0e2f5a;
  padding: 10px;
  color: #fff;
`;

const TopBar = () => <TopBarStyled>TopBar</TopBarStyled>;

export default TopBar;
